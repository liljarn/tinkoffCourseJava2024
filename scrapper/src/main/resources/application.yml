app:
  scheduler:
    enable: true
    interval: 5s
    force-check-delay: 5s

spring:
  application:
    name: scrapper
  liquibase:
    enabled: false

server:
  port: 8080

logging:
  config: classpath:log4j2-plain.xml

springdoc:
  swagger-ui:
    path: /swagger-ui

client:
  bot:
    base-url:
  github:
    base-url:
  stackoverflow:
    base-url:
